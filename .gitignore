# Dependencies & builds
node_modules/
dist/
dist-electron/
release/
*.tsbuildinfo

# Env / secrets — never commit keys
.env
.env.*
!.env.example
*.pem
*.p12
*.key
credentials*.json
**/app-data.json

# Local-only companion art overrides (optional)
src/assets/local/

# OS / editor
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Logs & caches
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.cache/
.parcel-cache/
.turbo/

# Python / local tooling (sprite experiments, etc.)
.venv/
.venv-*/
__pycache__/
*.pyc

# Electron / packaging leftovers
*.asar
*.dmg
*.zip
*.blockmap
builder-effective-config.yaml
builder-debug.yml

# Xcode (native macOS app)
# Scoped to the Xcode project rather than a bare `build/`, which also matched
# the top-level build/ holding electron-builder's icons and entitlements — those
# are packaging *inputs* and must stay tracked.
DerivedData/
TodoCompanion/build/
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout
*.moved-aside

# Old / unused asset dumps
public/ort/
sprites/
native/bin/

# Optional local notes
TODO.local.md
